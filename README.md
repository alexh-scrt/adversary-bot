# adversary-bot
adversary_bot is a developer tool that acts as a hostile, adversarial code reviewer for your pull requests. It uses an LLM (OpenAI or Anthropic Claude) to deliberately hunt for security vulnerabilities, logic flaws, race conditions, and design weaknesses — not style issues. It can be run locally against a diff or deployed as a GitHub Action that au
